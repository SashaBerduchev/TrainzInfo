@model IEnumerable<TrainzInfo.Models.DieselTrains>

@{
    ViewData["Title"] = "Дизель-поїзди";
}

<h1 class="mb-4 text-center">Дизель-поїзди</h1>

@if (ViewBag.user != null && (ViewBag.user.Role == "Publisher" || ViewBag.user.Role == "Admin" || ViewBag.user.Role == "Superadmin"))
{
    <a class="btn btn-info mb-3" asp-action="Create">Створити</a>
}

<form method="get" class="row g-2 mb-3">
    <div class="col-12 col-md-3">
        @Html.DropDownList("Filia", ViewBag.filia as SelectList, htmlAttributes: new { @class = "form-control" })
    </div>
    <div class="col-12 col-md-3">
        @Html.DropDownList("Oblast", ViewBag.oblast as SelectList, htmlAttributes: new { @class = "form-control" })
    </div>
    <div class="col-12 col-md-3">
        @Html.DropDownList("Depo", ViewBag.depo as SelectList, htmlAttributes: new { @class = "form-control" })
    </div>
    <div class="col-12 col-md-3">
        @Html.DropDownList("Model", ViewBag.model as SelectList, htmlAttributes: new { @class = "form-control" })
    </div>
    <div class="col-12 mt-2">
        <input type="submit" value="Відобразити" class="btn btn-info w-100" />
    </div>
</form>

<div class="mb-3">
    <a class="btn btn-secondary" asp-action="ClearFilter">Скинути фільтр</a>
</div>

<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-12 mb-4">
            <div class="card shadow-sm">
                <div class="row g-0">
                    <div class="col-12 col-md-4">
                        <img class="img-fluid rounded-start"
                             src="@Url.Action("GetImage", "DieselTrains", new { item.Id })"
                             alt="Дизель-поїзд" />
                    </div>

                    <div class="col-12 col-md-5 p-3">
                        <h5>@item.SuburbanTrainsInfo.Model — №@item.NumberTrain</h5>
                        <p><b>Депо:</b> @item.DepotList.Name</p>
                        <p><b>Філія:</b> @item.DepotList.UkrainsRailway.Name</p>
                        <p><b>Область:</b> @item.DepotList.City.Oblasts.Name</p>
                        <p><b>Місто:</b> @item.DepotList.City.Name</p>
                    </div>

                    <div class="col-12 col-md-3 d-flex flex-column justify-content-center p-3">
                        <a class="btn btn-info mb-2" asp-action="Details" asp-route-id="@item.Id">Детально</a>

                        @if (ViewBag.user != null && (ViewBag.user.Role == "Publisher" || ViewBag.user.Role == "Admin" || ViewBag.user.Role == "Superadmin"))
                        {
                            <a class="btn btn-outline-warning mb-2" asp-action="AddImageForm" asp-route-id="@item.Id">Додати зображення</a>
                            <a class="btn btn-success mb-2" asp-action="Edit" asp-route-id="@item.Id">Редагувати</a>
                            <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Видалити</a>
                        }
                    </div>
                </div>
            </div>
        </div>
    }
</div>

<style>
    body {
        background: url(https://klike.net/uploads/posts/2020-04/1586763576_18.jpg);
    }
</style>