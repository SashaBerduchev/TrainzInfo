@model TrainzInfo.Models.IpAdresses

@{
    ViewData["Title"] = "delete/{id}";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>IpAdresses</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.IpAddres)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.IpAddres)
        </dd>
    </dl>
    
    <form asp-action="delete/{id}">
        <input type="hidden" asp-for="id" />
        <input type="submit" value="delete/{id}" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
